const path = require('path');

module.exports = {
  'config': path.resolve('migrate','config', 'database.js'),
  'models-path': path.resolve('migrate', 'models'),
  'seeders-path': path.resolve('migrate', 'seeders'),
  'migrations-path': path.resolve('migrate', 'migrations')
}
